# DocStock

This project provides a simple Spring Boot API for user registration. It exposes an endpoint `/api/users/register` accepting user details and returning the created user without the password.

## Build and Test

Use Maven to build and run tests:

```bash
mvn test
```
